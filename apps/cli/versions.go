package main

// Scaffold dependency versions — all npm package versions for single-file deploys.

// Shared tooling (used across React, Vue, Svelte scaffolds)
const (
	VersionVite         = "^5"
	VersionTailwind     = "^3"
	VersionPostCSS      = "^8"
	VersionAutoprefixer = "^10"
	VersionTypeScript   = "^5"
)

// React
const (
	VersionReact           = "^18"
	VersionReactDOM        = "^18"
	VersionVitePluginReact = "^4"
	VersionTypesReact      = "^18"
	VersionTypesReactDOM   = "^18"
)

// Vue
const (
	VersionVue           = "^3"
	VersionVitePluginVue = "^5"
)

// Svelte
const (
	VersionSvelte           = "^4"
	VersionVitePluginSvelte = "^3"
)
